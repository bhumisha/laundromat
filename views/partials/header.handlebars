<nav class="navbar navbar-expand-md navbar-light bg-light navbar-fixed-top">
  <div class="container-fluid">
    <a class="navbar-brand ms-2 p-0" href="/">Laundry</a>
    <div class="flex-row-reverse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        {{#if loggedIn}}
        <a class="nav-link" aria-current="page" href="#">Place Order</a>
        <a class="nav-link" id="logout" href="/">Logout</a>
        {{else}}
        {{!-- <a class="nav-link" href="#">Sign-Up</a> --}}
        <a class="nav-link" href="/">Login</a>
        {{/if}}
      </div>
    </div>
  </div>
</nav>
{{#if loggedIn}}
  <script src="/js/cust_logout.js"></script>
{{/if}}